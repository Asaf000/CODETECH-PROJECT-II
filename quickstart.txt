# 🚀 Quick Start Guide

## Get Your Chat App Running in 5 Minutes!

### Step 1: Install Dependencies (1 minute)

```bash
pip install -r requirements.txt
```

### Step 2: Configure Database (1 minute)

1. Make sure MySQL is running
2. Open `.env` file
3. Update this line:
   ```env
   DB_PASSWORD=your_mysql_password_here
   ```

### Step 3: Initialize Database (1 minute)

```bash
python setup_db.py
```

You should see:
```
✅ Database and tables created successfully!
```

### Step 4: Run the Application (30 seconds)

```bash
python app.py
```

### Step 5: Open in Browser (30 seconds)

Go to: **http://localhost:5000**

---

## 🎯 First Time Setup

### Create Your Account:

1. Click **"Register here"**
2. Fill in:
   - Username: `testuser`
   - Email: `test@example.com`
   - Display Name: `Test User`
   - Password: `password123`
3. Click **"Create Account"**

### Login:

1. Enter username: `testuser`
2. Enter password: `password123`
3. Click **"Login"**

### Start Chatting:

1. Click on **"General"** room in sidebar
2. Type a message: "Hello, World!"
3. Press **Enter** to send

---

## 🎉 That's It!

You now have a fully functional real-time chat application!

### Try These Features:

✅ **Multiple Rooms** - Click on different rooms (General, Technology, Random)  
✅ **Create Room** - Click the "+" button to make your own room  
✅ **Typing Indicator** - Start typing to see the indicator  
✅ **Open Multiple Tabs** - Chat with yourself to test real-time features!  

---

## 🧪 Test Real-Time Features

### Open 2 Browser Windows:

1. **Window 1:** Login as `testuser`
2. **Window 2:** Register and login as `testuser2`
3. Both join **"General"** room
4. Send messages from both windows
5. Watch messages appear instantly! 🚀

---

## ⚠️ Troubleshooting

**Can't connect to MySQL?**
```bash
# Check if MySQL is running (Windows)
services.msc
# Look for "MySQL" and start it
```

**Port 5000 already in use?**
- Change port in `.env`:
  ```env
  SERVER_PORT=5001
  ```

**Dependencies not installing?**
```bash
# Try upgrading pip first
python -m pip install --upgrade pip
pip install -r requirements.txt
```

---

## 📚 What's Next?

- Read the full **README.md** for detailed documentation
- Check out the **Socket.IO events** section
- Explore the **database schema**
- Try **creating custom rooms**
- Test with **multiple users**

---

**Happy Chatting! 💬**

Need help? Check the README.md for full documentation!